apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
    app: ingress
    component: frontend
  name: ingress-webapi
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    # your subnets of instance, public subnets of EKS
    alb.ingress.kubernetes.io/subnets: subnet-074701c6e8779fb44,subnet-087a5c59991c0bf93,subnet-0fd7f133f8867928a
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}]'
    alb.ingress.kubernetes.io/security-groups: 'eks-alb'
spec:
  rules:
  - http:
      paths:
      - path: /catalog-api
        pathType: Prefix
        backend:
          service:
            name: catalog-api
            port:
              number: 80
      - path: /basket-api
        pathType: Prefix
        backend:
          service:
            name: basket-api
            port:
              number: 80

      - path: /order-api
        pathType: Prefix
        backend:
          service:
            name: order-api
            port:
              number: 80